server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Wugop/HowLongToEat-TP-Architecture-distribue
          search-paths: /configRepo
  profiles:
    active: native
